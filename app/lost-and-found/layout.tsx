import type React from "react"
export default function LostAndFoundLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <>
      {/* This is a minimal layout for the Lost and Found section */}
      {children}
    </>
  )
}
